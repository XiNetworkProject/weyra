# Weyra Atlas S4 — Build verification

- `npm run check` : passed
- `npm run build` : passed

S4 fixes the unsupported RainViewer zoom request and moves radar loading behind `/api/radar/timeline` for the Météo-France migration path.
